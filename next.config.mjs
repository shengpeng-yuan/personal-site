/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 如需更小的部署体积，可取消注释下面的配置，
  // 改为用 .next/standalone/server.js 启动（详见 DEPLOY.md 的「进阶」章节）
  // output: 'standalone',
};

export default nextConfig;
